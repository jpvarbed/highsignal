{"id": "a1", "kind": "planted", "context": "social", "expect_lens": "tells", "expect_category": "em-dash", "draft": "The rollout went fine — mostly — but the migration notes lagged a day — and on-call noticed.", "why": "Three em dashes in one short sentence. Individually the clauses are coherent, so only the tells lens should fire (em-dash); adjacency/paragraph/whole-arc have nothing to flag."}
{"id": "a2", "kind": "planted", "context": "social", "expect_lens": "adjacency", "expect_category": "non-sequitur", "draft": "We moved the scheduler to a priority queue and tail latency dropped by half. The kitchen restocked the oat milk on Thursday.", "why": "Two adjacent sentences with no logical link. Each sentence is clean on its own (no tells), it is one short paragraph (no paragraph-arc issue), and there is no whole-piece to mis-shape. Only adjacency: non-sequitur."}
{"id": "a3", "kind": "planted", "context": "social", "expect_lens": "adjacency", "expect_category": "broken-referent", "draft": "The schema migration and the read-replica cutover both went out this morning. It started returning stale rows around noon.", "why": "\"It\" has two candidate referents (migration vs cutover). Sentences are otherwise clean; the defect only exists across the sentence boundary. Only adjacency: broken-referent."}
{"id": "a4", "kind": "planted", "context": "long-form", "expect_lens": "paragraph-arc", "expect_category": "restates-previous", "draft": "Batching the writes cut our database round-trips from nine per request to two. The checkout endpoint went from 350ms to 90ms.\n\nBecause we batched the writes, there were far fewer round-trips to the database. Checkout got much faster as a result.", "why": "Paragraph 2 restates paragraph 1 with no new information. Every sentence is individually clean and locally coherent; the defect is only visible at paragraph granularity. Only paragraph-arc: restates-previous."}
{"id": "a5", "kind": "planted", "context": "long-form", "expect_lens": "whole-arc", "expect_category": "weak-opening", "draft": "We run the ingestion pipeline on a nightly cron. It reads from S3, normalizes the records, and writes to Postgres.\n\nThe job had been stable for about a year. Most nights it finishes in under twenty minutes.\n\nLast week it silently dropped 12% of records for six nights straight before anyone noticed, because a schema change made the normalizer skip rows it could not parse.", "why": "The real news (silent 12% data loss for six nights) is buried in the last paragraph; the piece opens with boring setup. Each sentence is clean, each paragraph is locally fine, adjacency is fine. The defect is only at the whole-piece level: weak-opening (lede buried). Fix is a restructure, not a sentence edit."}
{"id": "ac1", "kind": "clean", "context": "social", "expect_lens": null, "expect_category": null, "draft": "The deploy failed because the migration ran before the column existed. We reordered the steps and it went through.", "why": "Coherent, leads with the cause, no tells, no flow break, no arc problem. Every lens should emit []."}
{"id": "ac2", "kind": "clean", "context": "long-form", "expect_lens": null, "expect_category": null, "draft": "The backfill took three weeks, almost all of it waiting on the nightly export windows. The schema change itself landed in an afternoon.\n\nWe kept the old and new columns in sync with a trigger until the backfill caught up, then dropped the old one. No downtime, no data loss.", "why": "Realistic multi-paragraph sample. Each paragraph has one job and a shape, sentences follow, no tells, spine is clear. A near-miss for filler/paragraph-arc but genuinely clean. Every lens should emit []."}
